namespace = dmsd_suppress

# Silence "set but never used" warnings for variables read only via GUI.
# This event is never triggered; orphan = yes tells the engine it's intentional.

dmsd_suppress.1 = {
	type = country_event
	orphan = yes

	immediate = {
		change_variable = { name = dmsd_disp_court_reg_io multiply = 1 }
		change_variable = { name = dmsd_disp_court_rel_io multiply = 1 }
		change_variable = { name = dmsd_disp_diplo_io multiply = 1 }
		change_variable = { name = dmsd_disp_stab_io multiply = 1 }
		change_variable = { name = dmsd_disp_cult_io multiply = 1 }
		change_variable = { name = dmsd_cult_raw_gold multiply = 1 }
	}
}
