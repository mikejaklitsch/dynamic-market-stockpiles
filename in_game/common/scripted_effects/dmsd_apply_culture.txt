# Phase 2 cultural demand. Gold map keyed by reg1 demand types.
# Subcontinent only, no religion split.
# Runs in location scope with scope:dmsd_mkt available.

dmsd_apply_culture = {
	if = {
		limit = { has_variable_list = dmsd_cult_applied }
		every_in_list = {
			variable = dmsd_cult_applied
			save_temporary_scope_as = dmsd_old
			scope:dmsd_mkt = { remove_temporary_demand = scope:dmsd_old }
		}
		clear_variable_list = dmsd_cult_applied
	}

	if = {
		limit = { has_variable_map = dmsd_cult_map }
		every_key_in_variable_map = {
			variable = dmsd_cult_map
			save_temporary_scope_as = dmsd_demand

			scope:dmsd_mkt = {
				set_local_variable = { name = dmsd_bp value = 0 }

				set_global_variable = { name = dmsd_io_ref value = "global_variable_map(dmsd_demand_io|scope:dmsd_demand)" }
				global_var:dmsd_io_ref = {
					every_key_in_variable_map = {
						variable = dmsd_weights
						save_temporary_scope_as = dmsd_good
						scope:dmsd_mkt = {
							change_local_variable = {
								name = dmsd_bp
								add = { value = "global_var:dmsd_io_ref.variable_map(dmsd_weights|scope:dmsd_good)" multiply = "market_price(scope:dmsd_good)" }
							}
						}
					}
				}

				set_local_variable = { name = dmsd_gv value = "location.variable_map(dmsd_cult_map|scope:dmsd_demand)" }
				if = {
					limit = { local_var:dmsd_bp > 0 local_var:dmsd_gv >= 0.01 }
					if = {
						limit = { has_global_variable = dmsd_swap }
						add_temporary_demand = {
							type = "global_variable_map(dmsd_demand_swap|scope:dmsd_demand)"
							months = -1
							scale = { value = local_var:dmsd_gv divide = local_var:dmsd_bp }
						}
						location = { add_to_variable_list = { name = dmsd_cult_applied target = "global_variable_map(dmsd_demand_swap|scope:dmsd_demand)" } }
					} else = {
						add_temporary_demand = {
							type = scope:dmsd_demand
							months = -1
							scale = { value = local_var:dmsd_gv divide = local_var:dmsd_bp }
						}
						location = { add_to_variable_list = { name = dmsd_cult_applied target = scope:dmsd_demand } }
					}
				}
			}
		}
		clear_variable_map = dmsd_cult_map
	}
}
